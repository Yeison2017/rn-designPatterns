class DataUserRegistrationServiceSingleton {
  descripcion: string;

  constructor() {
    this.descripcion =
      "Define una pantalla de configuración que permite a los usuarios registrar un nombre y apellido mediante un formulario. Utiliza un servicio singleton para gestionar el registro del usuario. Al enviar el formulario, el usuario se guarda. Incluye un botón que permite navegar a una pantalla de perfil del usuario si hay uno registrado.";
  }
}

export default DataUserRegistrationServiceSingleton;
